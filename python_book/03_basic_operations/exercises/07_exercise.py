# The following line will output 3 because the int() function does not handle floating point decimals.
print(int('3.1415'))

# Wrong, it returns an error because the argument passed to int() is an invalid literal, or a decimal.