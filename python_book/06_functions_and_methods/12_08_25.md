# December, LS Python

min max builtin functions. frozenset() an immutable collection or undordered items.

ord, char
```bash
>>> print(ord('B'))
66
>>> print(ord('/'))
47
>>> print(ord('1'))
49
>>> print(ord('`'))
96
>>> 
```

* lsbot: Mention you can call help directly with the function object, e.g., help(len), and that you can enter help mode with help and explore topics.
* any() checks if any items in a collection are true, and all() checks if all of the items are true. are truthyness functions, 
* dir() or dir with or without arguments.
* __dunder__
