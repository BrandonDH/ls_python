# creating functions

say.py

# Scope
function scope - anything initialized inside a function can only be used inside the function. You cannot reference the identifier form outside the function.

# namespace
mapping of identifiers to objects. when refering to an identifier python looks at local namespace, or local scope, enclosing namespace, then the global namespace