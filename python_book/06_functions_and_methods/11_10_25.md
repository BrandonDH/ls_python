# Launch School Python 

`any` and all can be used for list comprehension - checking for truthyness for example `all([])` will return true becuase there is nothing `falsey` within the collelction.

does this list of numbers hav any even values?

```
numbers = [2, 5, 8, 10, 13]

print([number % 2 == 0 for number in numbers])
#[True, Fals True, True, False]
```

## Functions in the REPL
* identity `id()` gets an id number for the object in memory
* interning

```py
>>> a = 'gork'
>>> b= 'zork'
>>> id(a)
4338467056
>>> id(b)
4338470896
```